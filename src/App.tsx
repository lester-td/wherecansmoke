import { lazy, Suspense, useCallback, useMemo, useState } from 'react';
import { Crosshair, ExternalLink, Info, LocateFixed, MapPinned, ShieldCheck } from 'lucide-react';
import rawData from './data/dsa.geojson';
import { DsaCard } from './components/DsaCard';
import { Window } from './components/Window';
import { useGeolocation } from './hooks/useGeolocation';
import { distanceMetres, rankDsas } from './lib/geo';
import { validateDataset } from './lib/validate';
import type { DsaCollection } from './types/dsa';
const MapView=lazy(()=>import('./components/MapView'));
let dataError='';let data:DsaCollection={type:'FeatureCollection',features:[]};try{validateDataset(rawData);data=rawData;}catch(error){dataError=error instanceof Error?error.message:'Unknown dataset error';}
const ORCHARD_CENTRE={latitude:1.3048,longitude:103.8335};
export default function App(){const location=useGeolocation();const [selectedId,setSelectedId]=useState<number|null>(data.features[0]?.properties.OBJECTID??null);const ranked=useMemo(()=>location.position?rankDsas(data,location.position):[],[location.position]);const visible=ranked.slice(0,Math.max(3,ranked.length));const selected=ranked.find(item=>item.feature.properties.OBJECTID===selectedId);const chooseManual=useCallback((point:Parameters<typeof location.setManual>[0])=>{location.setManual(point);const first=rankDsas(data,point)[0];if(first)setSelectedId(first.feature.properties.OBJECTID);},[location.setManual]);const locate=()=>{location.locate();};const outside=location.position&&distanceMetres(location.position,ORCHARD_CENTRE)>5000;const lowAccuracy=location.position?.source==='device'&&(location.position.accuracy??0)>75;return <div className="app-shell"><header className="app-header"><a href="/code" className="back-link">← /code</a><div className="wordmark"><span className="signal-dot"/>WhereCanSmoke?</div><button className="icon-button" onClick={()=>document.getElementById('about')?.scrollIntoView({behavior:'smooth'})} aria-label="About this project"><Info size={18}/></button></header><main><section className="hero"><p className="eyebrow">SINGAPORE / DSA LOCATOR / UNOFFICIAL</p><h1>Find a place.<br/><span>Check the signs.</span></h1><p>Find the nearest designated smoking area in Singapore using your current location.</p><button className="mechanical primary" onClick={locate} disabled={location.status==='locating'}>{location.status==='locating'?<><Crosshair className="spin"/>Locating…</>:<><LocateFixed/>Use my location</>}</button><p className="privacy"><ShieldCheck size={14}/> Your coordinates stay in this tab. They are not stored, persisted or logged.</p></section>{dataError&&<div className="alert danger"><strong>Dataset failure</strong><span>{dataError}</span></div>}<div className={`status-strip ${location.status}`} role="status" aria-live="polite"><span className="status-light"/><div><strong>{location.status==='idle'?'READY FOR LOCATION':location.status.toUpperCase()}</strong><span>{location.message}</span></div>{location.position&&<code>{location.position.latitude.toFixed(5)}, {location.position.longitude.toFixed(5)}{location.position.accuracy?` · ±${Math.round(location.position.accuracy)} m`:''}</code>}</div>{lowAccuracy&&<div className="alert warning"><strong>LOW GPS ACCURACY</strong><span>Dense Orchard buildings can cause drift. Your reported accuracy is ±{Math.round(location.position!.accuracy!)} m; compare nearby results and use on-site signs.</span></div>}{outside&&<div className="alert warning"><strong>OUTSIDE EXPECTED AREA</strong><span>This dataset focuses on Orchard Road and your position is more than 5 km away. Results may not reflect the nearest DSA elsewhere in Singapore.</span></div>}<div className="dashboard"><Window title="AREA_MAP" meta={`${data.features.length} POINTS`} className="map-window"><Suspense fallback={<div className="map-loading">Loading map module…</div>}><MapView data={data} position={location.position} selectedId={selected?.feature.properties.OBJECTID??selectedId} onSelect={setSelectedId} onManualSelect={chooseManual}/></Suspense></Window><Window title="NEAREST_DSA" meta={location.position?'AIR-LINE RANK':'AWAITING FIX'} className="results-window">{!location.position?<div className="empty-state"><MapPinned size={34}/><strong>No start point</strong><p>Use your location, or tap the map to choose a point manually.</p></div>:<div className="results-list">{visible.map((item,index)=><DsaCard key={item.feature.properties.OBJECTID} item={item} index={index} selected={item.feature.properties.OBJECTID===(selected?.feature.properties.OBJECTID??selectedId)} onSelect={()=>setSelectedId(item.feature.properties.OBJECTID)}/>)}</div>}</Window></div><Window title="FIELD_NOTES" meta="READ BEFORE USE" className="notes"><div className="notes-grid"><div><strong>01 / DISTANCE</strong><p>Rankings use straight-line distance, not walking routes. Physical access may be longer or restricted.</p></div><div><strong>02 / VERIFY</strong><p>DSAs can be behind buildings, in service yards or on upper levels. Read the description, view the photo and verify signs on site.</p></div><div><strong>03 / COVERAGE</strong><p>The initial dataset is for the Orchard Road No Smoking Zone. It is not a complete smoking-law map of Singapore.</p></div></div></Window><section className="about" id="about"><p className="eyebrow">DATA & RESPONSIBILITY</p><h2>Useful reference.<br/>Not an official direction.</h2><p>WhereCanSmoke? is an independent, unofficial project. It is not affiliated with or endorsed by NEA, OneMap, SLA, or the Singapore Government.</p><div className="source-row"><span><b>Source snapshot</b>NEA Designated Smoking Areas via data.gov.sg</span><span><b>Dataset coverage</b>November 2025</span><span><b>Published</b>19 March 2026</span></div><div className="link-row"><a href="https://data.gov.sg/datasets/d_d0fa8f07ef80ab23feaa3b870323bf27/view" target="_blank" rel="noreferrer">DSA dataset <ExternalLink size={13}/></a><a href="https://www.nea.gov.sg/ORNSZ" target="_blank" rel="noreferrer">NEA ORNSZ <ExternalLink size={13}/></a><a href="https://www.onemap.gov.sg/" target="_blank" rel="noreferrer">OneMap <ExternalLink size={13}/></a></div></section></main><footer><span>WhereCanSmoke? / v0.1</span><span>DATA: NEA · MAP: ONEMAP/SLA</span></footer></div>}
