import { bearingDegrees, cardinalDirection, distanceMetres, rankDsas } from './geo';
import { describe, expect, it } from 'vitest';
import type { DsaCollection } from '../types/dsa';
const origin={latitude:1.3,longitude:103.8};
describe('geospatial utilities',()=>{it('returns zero distance for identical points',()=>expect(distanceMetres(origin,origin)).toBe(0));it('calculates a known approximate distance',()=>expect(distanceMetres({latitude:0,longitude:0},{latitude:0,longitude:1})).toBeCloseTo(111195,-1));it('calculates bearing and direction',()=>{expect(bearingDegrees(origin,{latitude:2.3,longitude:103.8})).toBeCloseTo(0,5);expect(cardinalDirection(91)).toBe('E');expect(cardinalDirection(359)).toBe('N')});it('ranks closest features first',()=>{const make=(id:number,latitude:number)=>({type:'Feature' as const,geometry:{type:'Point' as const,coordinates:[103.8,latitude] as [number,number]},properties:{OBJECTID:id,DESCRIPTION:'Place',BUILDING_N:`DSA ${id}`,PHOTOURL:'',Y:1,X:1,INC_CRC:'x',FMEL_UPD_D:'20250101000000'}});const data:DsaCollection={type:'FeatureCollection',features:[make(2,1.32),make(1,1.301)]};expect(rankDsas(data,origin).map(x=>x.feature.properties.OBJECTID)).toEqual([1,2])})});
