import type { DsaCollection } from '../types/dsa';
export function validateDataset(value: unknown): asserts value is DsaCollection { if(!value||typeof value!=='object') throw new Error('Dataset must be an object'); const data=value as Partial<DsaCollection>; if(data.type!=='FeatureCollection'||!Array.isArray(data.features)||data.features.length<1) throw new Error('Expected a non-empty FeatureCollection'); const ids=new Set<number>(); data.features.forEach((feature,index)=>{ if(feature?.type!=='Feature'||feature.geometry?.type!=='Point'||!Array.isArray(feature.geometry.coordinates)||feature.geometry.coordinates.length!==2) throw new Error(`Feature ${index} is not a Point`); const [lon,lat]=feature.geometry.coordinates; if(!Number.isFinite(lon)||!Number.isFinite(lat)||lon<103.5||lon>104.1||lat<1.1||lat>1.6) throw new Error(`Feature ${index} has coordinates outside Singapore`); const p=feature.properties; if(!p||!Number.isInteger(p.OBJECTID)||typeof p.BUILDING_N!=='string'||!p.BUILDING_N.trim()||typeof p.DESCRIPTION!=='string'||!p.DESCRIPTION.trim()||typeof p.PHOTOURL!=='string'||typeof p.FMEL_UPD_D!=='string') throw new Error(`Feature ${index} is missing required properties`); if(ids.has(p.OBJECTID)) throw new Error(`Duplicate OBJECTID ${p.OBJECTID}`); ids.add(p.OBJECTID); }); }
